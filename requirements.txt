garminconnect>=0.3.2,<0.4
fastmcp>=3.1.0
python-dotenv>=1.0.0
requests>=2.31.0
anthropic>=0.109

# Optional: browser-based Garmin login fallback (needed when Cloudflare blocks SSO)
# pip install playwright && playwright install chromium
playwright>=1.40.0
